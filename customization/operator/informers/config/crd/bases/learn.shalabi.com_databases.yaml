---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: databases.learn.shalabi.com
spec:
  group: learn.shalabi.com
  names:
    kind: Database
    listKind: DatabaseList
    plural: databases
    singular: database
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Database is the Schema for the databases API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DatabaseSpec defines the desired state of Database
            properties:
              cpu:
                description: CPU is the number of cpus wil be used per replica
                type: integer
              description:
                description: Description is a database description
                type: string
              memory:
                description: Memory is the memorory per GB per replica
                type: integer
              replicas:
                description: Replicas is the number of database replication
                type: integer
              storage:
                description: Storage is the number of harddisk spce used per replica
                  in GB
                type: integer
            type: object
          status:
            description: DatabaseStatus defines the observed state of Database
            properties:
              created:
                type: boolean
              healthy:
                description: Description is a database description
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
